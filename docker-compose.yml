services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: spdf
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true
